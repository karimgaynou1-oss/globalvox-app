// Canonical implementation lives in src/features/home/HomeScreen.tsx.
// This re-export preserves backward compatibility for any existing import paths.
export { default } from '../features/home/HomeScreen';
